# LabSense Backend Dependencies
# Minimum versions chosen to guarantee pre-built binary wheels for Python 3.13
# on Windows (amd64). No C compiler or Rust toolchain required.

fastapi>=0.115.0
uvicorn[standard]>=0.30.0
asyncpg>=0.30.0
pydantic>=2.10.0
pydantic-settings>=2.6.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.2.0
python-multipart>=0.0.9
websockets>=13.0
httpx>=0.27.0
# Windows ships no system tz database, so zoneinfo.ZoneInfo("Asia/Kolkata")
# raises without this. Required wherever the backend runs on Windows.
tzdata>=2024.1
pytest>=8.3.0
pytest-asyncio>=0.24.0
